#!/usr/bin/env zsh

selected="$(find "$NOTES" -mindepth 1 -maxdepth 1 -type f | sort -r | fzf --bind 'ctrl-f:execute-silent(open -b "com.apple.finder" {})+close' --bind 'ctrl-p:execute-silent(echo {})+close')"

[[ -z $selected ]] && return 1

"${EDITOR:-code}" "$selected"

